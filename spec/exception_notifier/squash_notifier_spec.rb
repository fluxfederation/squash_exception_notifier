require 'spec_helper'

describe ExceptionNotifier::SquashNotifier do
  it 'has a version number' do
    expect(ExceptionNotifier::SquashNotifier::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
